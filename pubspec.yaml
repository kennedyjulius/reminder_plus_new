name: reminder_plus
description: Voice Reminder+ - A beautiful reminder app with voice commands
version: 1.0.10+10

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  
  # Firebase
#  firebase_core: ^2.24.2
#  firebase_auth: ^4.15.3
#  cloud_firestore: ^4.13.6
#  google_sign_in: ^6.2.1

#  firebase_core: ^2.24.0
#  firebase_auth: ^4.17.0
#  cloud_firestore: ^4.15.0
#  google_sign_in: ^6.1.5

  firebase_core: ^3.5.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.3
  google_sign_in: ^6.2.1
#  sign_in_with_apple: ^5.0.0
  sign_in_with_apple: ^7.0.1
  
  # Animations and UI
  animated_splash_screen: ^1.3.0
  lottie: ^2.7.0
  
  # Local notifications and TTS
  flutter_local_notifications: ^17.2.3
  flutter_tts: ^3.8.5
  speech_to_text: ^7.0.0
  
  # HTTP requests
  http: ^1.1.2
  
  # Email API integrations
  googleapis: ^11.4.0
  googleapis_auth: ^1.4.1
  flutter_appauth: ^6.0.4
  
  # Natural language processing
  chrono: ^0.1.0
  
  # State management
  provider: ^6.1.1
  
  # Utilities
  shared_preferences: ^2.2.2
  timezone: ^0.9.2
  
  # Background tasks - using enhanced scheduled notifications
  # workmanager: ^0.5.1  # Temporarily disabled due to compatibility issues
  
  # File picker for custom sounds
  file_picker: ^8.0.0+1
  
  # Audio player for sound preview
  audioplayers: ^5.2.1
  
  # Send email via the user's email app (opens composer)
  flutter_email_sender: ^6.0.0
  
  # OCR and image processing
#  google_ml_kit: ^0.16.3
  image_picker: ^1.0.4
  google_ml_kit: ^0.20.0

  
  # Date parsing
  intl: ^0.19.0
  
  # Google Calendar integration
  
  # HTTP requests
  
  # OAuth and authentication
  oauth2: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    # - assets/icons/
    # - assets/images/
    - assets/sounds/
